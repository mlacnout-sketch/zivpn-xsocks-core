name: ZIVPN xSocks Core Build

on:
  push:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  build:
    name: Compile Native & Build Release APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Java Environment
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Android Environment
        uses: android-actions/setup-android@v3

      - name: Install NDK & CMake
        run: |
          yes | sdkmanager --licenses
          sdkmanager "ndk;27.0.12077973" "cmake;3.18.1"
      - name: Native Code Sanity Check (Fail Fast)
        run: |
          # Manually trigger CMake build to catch C/C++ errors early
          mkdir -p native/build
          cd native/build
          
          echo "Building for armeabi-v7a..."
          cmake .. -DCMAKE_TOOLCHAIN_FILE=$ANDROID_HOME/ndk/27.0.12077973/build/cmake/android.toolchain.cmake -DANDROID_ABI=armeabi-v7a -DANDROID_PLATFORM=android-24 -DCMAKE_BUILD_TYPE=Release -DSKIP_JNI_CHECK=ON
          make -j$(nproc)
          
          rm -rf * # Clean build
          
          echo "Building for arm64-v8a..."
          cmake .. -DCMAKE_TOOLCHAIN_FILE=$ANDROID_HOME/ndk/27.0.12077973/build/cmake/android.toolchain.cmake -DANDROID_ABI=arm64-v8a -DANDROID_PLATFORM=android-24 -DCMAKE_BUILD_TYPE=Release -DSKIP_JNI_CHECK=ON
          make -j$(nproc)
          
          echo "Native compilation successful!"
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Clean & Get Flutter Dependencies
        run: |
          flutter clean
          flutter pub get
      - name: Flutter Analyze (Best Practice Check)
        run: flutter analyze

      - name: Decode Signing Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ ! -z "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 --decode > android/app/release.jks
            echo "Keystore decoded successfully."
          fi
      - name: Build Release APKs
        env:
          KEYSTORE_PATH: release.jks
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
        run: |
          # 1. Build Split APKs (v7a & v8a)
          flutter build apk --release --split-per-abi --target-platform android-arm,android-arm64
          
          # Pencarian file yang lebih fleksibel untuk menghindari kesalahan nama versi Gradle
          find build/app/outputs/flutter-apk/ -name "*v7a*release.apk" -exec cp {} build/app/outputs/flutter-apk/ZIVPN-v7a-release.apk \;
          find build/app/outputs/flutter-apk/ -name "*v8a*release.apk" -exec cp {} build/app/outputs/flutter-apk/ZIVPN-v8a-release.apk \;
          
          # 2. Build Universal APK
          flutter build apk --release --target-platform android-arm,android-arm64
          cp build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/ZIVPN-Universal-release.apk
          
          # Verifikasi keberadaan file
          ls -lh build/app/outputs/flutter-apk/ZIVPN-*.apk

      - name: Upload v7a APK
        uses: actions/upload-artifact@v4
        with:
          name: ZIVPN-v7a-Release
          path: build/app/outputs/flutter-apk/ZIVPN-v7a-release.apk

      - name: Upload v8a APK
        uses: actions/upload-artifact@v4
        with:
          name: ZIVPN-v8a-Release
          path: build/app/outputs/flutter-apk/ZIVPN-v8a-release.apk

      - name: Upload Universal APK
        uses: actions/upload-artifact@v4
        with:
          name: ZIVPN-Universal-Release
          path: build/app/outputs/flutter-apk/ZIVPN-Universal-release.apk

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: ZIVPN-*-Release
          merge-multiple: true

      - name: Create Release Tag
        id: tag_version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | head -n 1 | awk '{print $2}' | cut -d '+' -f 1)
          if [ -z "$VERSION" ]; then VERSION="1.0.0"; fi
          TAG="v$VERSION-b${{ github.run_number }}"
          echo "VERSION=$TAG" >> $GITHUB_ENV
          echo "Generated version: $TAG"
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.VERSION }}
          name: ZIVPN xSocks Core ${{ env.VERSION }}
          body: |
            ### 🚀 Optimized Release ${{ env.VERSION }}
            
            Build otomatis untuk ZIVPN xSocks Core dengan optimasi performa native dan perbaikan akurasi trafik.
            
            ### 📦 Daftar Artefak (APK):
            - **ZIVPN-v8a-release.apk** : Direkomendasikan untuk HP modern (64-bit). Lebih cepat dan efisien.
            - **ZIVPN-v7a-release.apk** : Untuk HP lama (32-bit). Kompatibilitas luas.
            - **ZIVPN-Universal-release.apk** : Mendukung semua arsitektur ARM (Ukuran file lebih besar).
            
            ### 🛠️ Key Improvements:
            - **Fixed:** Akurasi RX/TX dashboard (menggunakan interface tun0).
            - **Optimized:** Manajemen memori UDPGW dan efisiensi CPU Pdnsd.
            - **Feature:** Auto Pilot shortcut di Dashboard dengan animasi.
            - **Robust:** Auto update via VPN (SOCKS5 + Remote DNS).
            
            Run ID: `${{ github.run_id }}`
          files: |
            ZIVPN-v7a-release.apk
            ZIVPN-v8a-release.apk
            ZIVPN-Universal-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
